"use server"

/** @deprecated Prefer `processIntakeUploads` â€” alias kept for older imports. */
export { processIntakeUploads as runMockIntakeProcessing } from "@/app/actions/process-intake-uploads"
